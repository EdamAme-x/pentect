# Copy to .pentect/config.toml.

plugins = []
# Plugin runtime data is stored outside the repository in OS user data.

[handles]
scope = "device"

[agent]
required = false

[image]
ocr = true
redaction = "black"
unscanned = "block"

[files]
remember = true
