[package]
name = "openai-privacy-filter"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pentect-plugin = { path = "../../sdk/rust/pentect-plugin" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
