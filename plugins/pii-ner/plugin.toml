schema = "pentect.plugin.v1"
name = "pii-ner"
description = "Local ONNX PII named-entity recognition."
binary = "pentect-pii-ner"
repository = "EdamAme-x/pentect"

[publisher]
workflow = ".github/workflows/release.yml"

[execution]
runtime = "native"
mode = "persistent"

[middleware]
stages = ["detect"]
permissions = ["input:read"]
required = false
