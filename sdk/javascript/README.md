# @pentect/plugin

Dependency-free JavaScript helpers for a Pentect Command plugin.

```js
import {serve} from '@pentect/plugin';

await serve(request => ({spans: []}));
```

See <https://pentect.dev/plugins/command/>.
