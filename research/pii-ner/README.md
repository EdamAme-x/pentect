# PII NER research assets

These model and tokenizer assets came from the retired native `pii-ner`
plugin. They are not an installable Pentect plugin and are not shipped in
Pentect releases.

They are retained only for evaluating a future WebAssembly-compatible
inference implementation. Any replacement must publish a portable `.wasm`
module and use the same sandbox and approval path as every other executable
plugin.
