pub(crate) const HANDLE_CONTRACT: &str = "Pentect replaced sensitive values in this request with opaque local handles formatted as <<LABEL_HASH>>. The represented values were not sent to you. Preserve each handle byte-for-byte. When a local client tool or function needs a represented value, pass the unchanged handle in the corresponding input; Pentect resolves known handles only at that completed local tool boundary. Do not alter, expand, guess, expose, or look up the represented value in files or environment variables.";
