pub(crate) const HANDLE_CONTRACT: &str = r#"Pentect replaced local sensitive values in this request with placeholders such as <<LABEL_HASH>>. The original value is not visible to the model. When a local tool needs one, copy the placeholder unchanged into that tool's input; the local Pentect process can replace a known placeholder immediately before execution. Do not print or guess the original value."#;
