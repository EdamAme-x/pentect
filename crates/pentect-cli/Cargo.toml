[package]
name = "pentect-cli"
version = "0.0.1"
description = "Pentect CLI: mask | restore | resolve (stdin -> stdout)."
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "pentect"
path = "src/main.rs"

[dependencies]
pentect-core = { path = "../pentect-core" }
serde_json = { workspace = true }
