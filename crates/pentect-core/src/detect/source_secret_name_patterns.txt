# Source-code string literals that name a secret setting instead of containing
# the secret itself.
#
# Format:
# - compact:pattern
#   Match after identifier normalization and underscore removal. This covers
#   common API/OAuth names whether they appear as access_token, AccessToken, or
#   accesstoken.
# - allowed_component:word
#   Components allowed in compositional documentation placeholders.
# - sensitive_component:word
#   At least one of these must be present for compositional matching.
#
# Policy:
# - This file is consulted only by KeyValueDetector after the left side has a
#   source-code shape and the value is a compact identifier with no digits.
# - Do not add vendor names, benchmark project names, or real-looking sentinel
#   constants here. Values such as PROD_SECRET_VALUE and CustomToken must stay
#   detectable.
compact:api_key
compact:access_token
compact:refresh_token
compact:id_token
compact:client_secret
compact:client_key
compact:oauth_secret
compact:oauth_token
compact:my_password
compact:my_token
compact:test_auth_token
compact:test_access_token

allowed_component:my
allowed_component:test
allowed_component:sample
allowed_component:bot
allowed_component:api
allowed_component:access
allowed_component:refresh
allowed_component:id
allowed_component:client
allowed_component:oauth
allowed_component:auth
allowed_component:private
allowed_component:key
allowed_component:token
allowed_component:secret
allowed_component:password
allowed_component:passwd
allowed_component:credential

sensitive_component:key
sensitive_component:token
sensitive_component:secret
sensitive_component:password
sensitive_component:passwd
sensitive_component:credential
