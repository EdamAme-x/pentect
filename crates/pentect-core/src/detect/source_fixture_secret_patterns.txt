# Source-code fixture secret values.
#
# Format:
# - key_component:word
#   The normalized left-side identifier must contain one of these components.
# - value_exact:pattern
# - value_prefix:pattern
# - value_suffix:pattern
# - value_contains:pattern
#
# Policy:
# - This file is used only after KeyValueDetector has proven a source-code shaped
#   left side. It is not used for .env/config values.
# - These values are weak or synthetic credentials that are useful in tests and
#   examples but should not be globally suppressed. `password=pass`,
#   `password=secret`, and `password=letmein123` must still detect outside a
#   fixture key context.
key_component:mock
key_component:expected
key_component:fake
key_component:fixture
key_component:sample
key_component:dummy
key_component:test

value_exact:pass
value_exact:secret
value_exact:basic_pass
value_exact:letmein123
value_contains:let_me_in
value_prefix:at_
value_prefix:rt_
