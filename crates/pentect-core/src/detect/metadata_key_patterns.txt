# Structured metadata keys whose long opaque-looking values are identifiers.
#
# Format: kind:pattern
# Matching is done after identifier normalization. Keep this list to fields that
# are stable metadata IDs or hashes by convention; do not add generic "url" or
# "token" fields here.
#
# x5c is the X.509 certificate chain member used by JOSE/JWK-style payloads
# (RFC 7517 names the parameter; RFC 7515 also uses it for JWS headers). The
# certificate blob is opaque and high-entropy-looking, but it is public material;
# private keys remain covered by PEM/key detectors.
exact:node_id
exact:sha
exact:commit_sha
exact:tree_sha
exact:blob_sha
exact:etag
exact:if_none_match
exact:x5c
